<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.its.modules.app.dao.AdverBuildingDao">
    
	<sql id="adverBuildingColumns">
		a.adv_id AS "advId",
		a.village_line_id AS "villageLineId",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="adverBuildingJoins">
	</sql>
    
	<select id="get" resultType="AdverBuilding">
		SELECT 
			<include refid="adverBuildingColumns"/>
		FROM adver_building a
		<include refid="adverBuildingJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="AdverBuilding">
		SELECT 
			<include refid="adverBuildingColumns"/>
		FROM adver_building a
		<include refid="adverBuildingJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="AdverBuilding">
		SELECT 
			<include refid="adverBuildingColumns"/>
		FROM adver_building a
		<include refid="adverBuildingJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO adver_building(
			adv_id,
			village_line_id,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{advId},
			#{villageLineId},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE adver_building SET 	
			adv_id = #{advId},
			village_line_id = #{villageLineId},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE adver_building SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>